# Kirito Emby缺集自动订阅

MoviePilot V2/V3 插件。它会读取 Emby 中的电视剧、季和已有集数，通过 TMDB 判断已播缺集，然后调用 MoviePilot 原生订阅流程。

配置 Emby 地址、用户 ID 和 API Key 后，可按周期扫描，也可勾选“保存后立即扫描一次”。插件只创建季订阅，后续搜索、下载和整理由 MoviePilot 处理。

配置页采用统一工作区布局，包含连接配置、扫描规则、运行统计和立即扫描操作。最近处理的订阅会保存在插件数据目录中，并显示剧集海报、年份、季度、缺失集数和处理状态。
